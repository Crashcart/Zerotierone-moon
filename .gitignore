.claude/settings.local.json
.env
# Timestamped .env backups created on every update — contain secrets
.env-backups/
# Generated by install.sh with site-specific IPs — do not commit
docker-compose.yml
# AI-rules synced content — refresh with: bash .ai-rules/scripts/sync-rules.sh
.ai-rules/
