name: Unfreeze LLM Model

# Placeholder workflow — not applicable to this repo (no LLM component).
# Kept for governance framework consistency across Crashcart repos.
on:
  workflow_dispatch:

jobs:
  not-applicable:
    runs-on: ubuntu-latest
    steps:
      - name: Not applicable
        run: echo "ℹ️ This workflow is not applicable to zerotierone-moon (no LLM component)."
